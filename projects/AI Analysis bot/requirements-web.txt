# Web dashboard dependencies. The Telegram bot (bots/) has its own
# requirements in bots/main.py imports — do not double-install.
# These are everything the web/ tree needs on top of the system Python.

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.4
psycopg[binary]>=3.2.0
bcrypt>=4.2.0
itsdangerous>=2.2.0
python-multipart>=0.0.12

# Phase 2: outbound email for password reset.
resend>=2.0.0

# Optional dev tooling.
watchfiles>=0.24.0